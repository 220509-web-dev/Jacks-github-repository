//package com.revature.quizzzard.services;

//import com.revature.quizzzard.entities.User;
//import com.revature.quizzzard.utils.exceptions.InvalidRequestException;

//public class UsersService {

    //public Users createNewUser(Users newUser) {

 //       private final UsersDAO cardDAO;

 //       public UsersService (UsersDao useridDAO) {
 //           this.userDAO = userDAO;
//       }


            //validate that the data provided from the web layer
//        if (newUser == null || newUser.getusername() == null || newUser.getUsername().equals("") ||
 //       newUser.getPassword() == null || newUser.getPassword().equals(""))
 //       {
  //          String msg = "Provided user data was invalid. username and password info must not be null or empty.";
  //          //logger.log(msg, loglevel.ERROR)
  //          throw new InvalidRequestException(msg);
  //      }

        //if blaid persist



  //  }
//}